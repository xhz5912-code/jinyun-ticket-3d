💗 温馨提示：项目文件夹创建一个models文件夹，里面放置自己喜欢的glb模型文件，就可以粒子切换自己喜欢的模型了

提示词如下：
# 任务：手势控制的"奥术能量舱"粒子猫装置（浏览器版）

做一个网页互动装置：摄像头画面前悬浮一个魔法能量立方体，里面关着一只由
GPU 粒子组成的发光猫。手势可以移动容器、切换猫（触发华丽转场）、缩放、熄灭。

本提示词已把所有难点的解法写死。严格照做，不要改架构；只允许在美术细节上发挥。
按"构建阶段"顺序做，每个阶段先通过验收再进入下一阶段。

═══════════════════════════════════════════
## 0. 技术栈与硬性约束
═══════════════════════════════════════════
- Vite + TypeScript + three（0.18x）+ @mediapipe/tasks-vision（0.10.x），不用框架
- 目录：src/{core, hand-tracking, gestures, particle-system, transitions,
  container, models, ui}，shader 用独立 .glsl 文件 + ?raw 导入
- 资源全部本地化，运行时零外域请求：MediaPipe 的 wasm 目录和
  hand_landmarker.task 放 public/mediapipe/；GLTFLoader 的 Draco 解码器放
  public/decoders/draco/、KTX2(basis) 放 public/decoders/basis/
- 模型清单：写一个构建前脚本扫描 public/models/*.glb 生成
  public/models/manifest.json（浏览器无法枚举目录）；npm run dev/build 前自动跑

═══════════════════════════════════════════
## 1. 十二条铁律（每一条都是真实翻过的车，违反即返工）
═══════════════════════════════════════════
1. WebGL 画布必须不透明黑底，用 CSS `mix-blend-mode: plus-lighter`
   （@supports 不支持时回退 screen）叠加到视频上。
   禁止透明画布 + UnrealBloom —— 会产生黑边光晕伪影。
2. 加法混合下黑色 = 隐形。黑猫模型直接采样颜色会整只消失。
   必须使用第 4.2 节的颜色重映射公式，一字不改地抄。
3. UnrealBloom：threshold 0.72 / strength 0.58 / radius 0.55。
   threshold 任何时候不得低于 0.6，否则整屏进 bloom 糊成光雾。
4. AfterimagePass（拖尾）：待机时必须彻底禁用（damp=0 且 pass.enabled=false）；
   转场主特效 ≤0.68、故障转场 0.45、蓄力 0.25、重组 0.35*(1-p)、坍缩 0.6。
   原理：加法混合下拖尾亮度会累积到 1/(1-damp) 倍，damp 0.9 = 7 倍 = 白屏。
5. 粒子必须与容器盒体碰撞（第 4.3 节代码），并且 position pass 末尾
   必须 clamp 到盒内。禁止让爆发粒子穿墙飞出容器——立方体是主角不是摆设。
6. 所有爆发冲量用宽 bell 包络：exp(-((phase-c)/w)²)，w ≥ 0.09。
   禁止窄于 3 帧的脉冲——帧率不同时冲量会随机丢失。
7. 禁止把大量粒子重生在一个点上（如黑洞喷发）。重生半径 ≥0.06 的小云团，
   否则几万粒子挤在一个像素 = 超新星白屏。
8. 捏合(pinch)手势只在 openScore > 0.42 时允许判定。
   原因：握拳时拇指尖天然贴着食指尖，不加此规则握拳会被误判成捏合，
   导致"握拳→张开切换"永远失灵还乱缩放。
9. 容器是 sticky 的：左掌张开召唤一次后**常驻**，手放下/跟踪丢失时容器原地
   停留，只有双拳坍缩才熄灭。禁止"松手即消失"（举着手臂太累）。
10. 粒子散开度 spread = smoothstep(openScore, 0.9, 1.0) * 0.55，带死区。
    禁止线性映射 (openScore-0.54)/0.46 —— 因为容器要求张手才召唤，
    线性映射会让猫永远处于半散开的雾状。
11. 粒子状态纹理默认 RGBA32F（检测 EXT_color_buffer_float + 试渲染探针，
    失败才回退 HalfFloat）。半精度会让待机微动出现量化台阶。
12. 视觉验收不得只看鼠标模式默认状态：必须用 Shift+滚轮把 spread 拉满截图、
    逐一强制 9 种转场截图确认。鼠标模式默认参数下"看起来不错"不代表
    摄像头模式没问题。

═══════════════════════════════════════════
## 2. 构建阶段（顺序执行，逐段验收）
═══════════════════════════════════════════
阶段 A 静态粒子猫：GPGPU + 模型采样 + 颜色管线 + bloom。
  验收：黑色模型的猫清晰可见呈"深色发光体"，白色模型不烧成死白，60fps。
阶段 B 能量舱：线框 + 护罩 + 法阵 + 边缘脉冲 + 舞台暗幕。
  验收：正对时护罩几乎透明不挡猫，侧掠角发亮，法阵缓慢旋转。
阶段 C 九种转场 + 盒体碰撞：验收：任意转场中粒子零穿墙，撞壁处护罩起涟漪。
阶段 D 手势：MediaPipe worker + 状态机 + 防呆三规则。
  验收：手势单元测试全绿（含"紧握拳不得判定为捏合"回归测试）。
阶段 E 打磨：启动页、HUD、鼠标降级、性能分档、全部测试 + 截图验收。

═══════════════════════════════════════════
## 3. 画面分层（DOM，从底到顶）
═══════════════════════════════════════════
1. <video> 镜像摄像头，object-fit:cover，
   filter: saturate(0.58) contrast(1.12) brightness(0.6)
2. 暗角层：radial-gradient 四周压暗 + 细噪点纹理(soft-light)
3. 舞台暗幕：一个 div，径向渐变黑（中心 92% → 55%处 66% → 边缘透明），
   mix-blend-mode: multiply。每帧把容器中心用相机 project() 投到屏幕像素，
   直径 = 容器世界直径×2.3 换算像素，跟随移动。这是"猫清晰可读"的关键：
   给加法粒子一个局部黑暗舞台。
4. WebGL 画布（黑底，plus-lighter，见铁律 1）
5. UI 层（HUD / 提示 / 启动页）
摄像头拒绝或不可用 → 自动进入鼠标模式（背景换成静态氛围图或纯暗色）。

═══════════════════════════════════════════
## 4. GPGPU 粒子核心
═══════════════════════════════════════════
档位（纹理无填充精确布局）：
  5万=250×200（移动端默认：pointer coarse 或 min(宽,高)<720）
  20万=500×400（桌面默认）   50万=1000×500（手动高档）
GPUComputationRenderer，position/velocity 两变量互相依赖 ping-pong。
velocity.w = 能量通道（发光度），position.w = 粒子种子/黑洞重生锁存。
渲染：Points + ShaderMaterial，AdditiveBlending，depthWrite:false，
点精灵 = 亮核(smoothstep 0→0.34) + 软晕(smoothstep 0.3→1)，
半径>1 discard。点大小：0.034/0.021/0.0145（按档位），透视缩放
300/max(-viewZ,0.25)，×(0.8+seed*0.45)×(1+energy*0.5)，clamp(1.25, 64)。
透明度：0.32/0.16/0.08 ×(0.72+seed*0.28)。
后期链：Render → Afterimage → UnrealBloom → 色散/氛围 ShaderPass → Output。
色散 pass 常驻但 amount 默认 0，故障转场时脉冲式拉高；同 pass 里加一点
主题色氛围光（amount 0.014，转场时×3）。ACES tone mapping，exposure 1.05。

### 4.2 颜色管线（顶点着色器，逐字照抄——解决"黑猫隐形"）
uniform vec3 uThemeOld, uThemeNew;  // 每只猫一个主题色（见第 7 节）
vec3 albedo = mix(oldColor, newColor, colorMix);
vec3 theme  = mix(uThemeOld, uThemeNew, colorMix);
float luma  = dot(albedo, vec3(0.2126, 0.7152, 0.0722));
vec3 chroma = albedo / max(luma, 0.045);          // 保色相
float lifted = 0.26 + 0.6 * pow(luma, 0.6);       // 亮度重映射进可见区间
vec3 fur = chroma * lifted;
float darkness = 1.0 - smoothstep(0.02, 0.17, luma);
fur = mix(fur, theme * (0.5 + seed * 0.3), darkness * 0.88);
// 近黑毛色→主题色辉光：黑猫成为"深色发光生物"而不是消失
另加：1.5% 的粒子随机闪烁（星尘感）：intensity += step(0.985, hash) * sin 波动

### 4.3 盒体碰撞（velocity pass 末尾，照抄——解决"穿墙"+制造"洪水撞击"）
float wallLimit = halfSize * 0.985;
float restitution = 待机0.34 / 蓄力0.42 / 主特效0.74 / 重组坍缩0.26;
vec3 predicted = position + velocity * dt;
float impact = 0.0;
// 三轴各一段（x 示例，y/z 同理）：
if (abs(predicted.x) > wallLimit && predicted.x * velocity.x > 0.0) {
  impact = max(impact, abs(velocity.x));
  velocity.x = -velocity.x * restitution;   // 反弹
  velocity.yz *= 0.985;                     // 切向摩擦→贴壁滑流
}
energy = min(2.2, energy + impact * 0.12);  // 撞击写入能量→变大变亮
// position pass 最后一行：
nextPosition = clamp(nextPosition, vec3(-halfSize*0.992), vec3(halfSize*0.992));

### 4.4 贴壁环流（主特效阶段追加，洪水感的来源）
float wallDepth = halfSize - max(|x|, max(|y|, |z|));
float hugging = 1.0 - smoothstep(0.0, 0.26, wallDepth);
force += normalize(cross(position, vec3(0.14,1.0,0.21))) * hugging * swirlGain;
// swirlGain：星云 6.5 / 烟花 7.5 / 黑洞 5 / 龙卷 9 / 海浪 4 / 其余 0
// 重组前 20% 相位渐退到 0

═══════════════════════════════════════════
## 5. 容器 = 奥术能量舱（四件套 + 两个接口）
═══════════════════════════════════════════
一个 Group 统一承载，整体跟随手掌/鼠标（位置阻尼 1-exp(-dt*15)，旋转限 ±20° 可调）：
1. 线框：Line2 每边双层——辉光层 7.2px 透明度 0.26 + 内芯 1.55px 0.88；
   8 角发光球 sprite（径向渐变贴图），呼吸微缩放；召唤时 12 边错峰生长展开。
2. 能量护罩：一个 BoxGeometry(2H) + DoubleSide ShaderMaterial（加法混合）：
   - fresnel = pow(1 - |dot(N, viewDir)|, 2.5) → 正对≈透明（不挡猫），侧掠发亮
   - 流动六边形网格（照抄）：
     float hexEdge(vec2 uv, float s){ vec2 p=uv*s, h=vec2(1.,1.7320508);
       vec2 a=mod(p,h)-h*.5, b=mod(p-h*.5,h)-h*.5;
       vec2 g=dot(a,a)<dot(b,b)?a:b; g=abs(g);
       return smoothstep(.4,.5,max(dot(g,vec2(.8660254,.5)),g.y)); }
     面 UV 按法线主轴取局部坐标另两轴；缓慢漂移；亮度乘活跃度
   - 撞击涟漪：uniform vec4 uRipples[8]（xyz=盒局部坐标，w=起始时间）+
     强度数组，环 = exp(-((dist - age*2.7)/0.13)²) * exp(-age*2.4)；
     用盒局部 3D 距离→涟漪会自然翻过棱角跨面传播，很帅
   - 一条缓慢上下扫描的微亮横带
3. 符文法阵：盒底下方两张平放 Plane，程序化 1024 canvas 贴图
   （同心圆环+96 刻度+24 个卢恩字符 ᚠᚢᚦᚨᚱᚲ…+六芒弦线 / 内环点阵+虚线环），
   反向旋转（0.12 与 -0.2 rad/s），转场时转速×4.4、亮度×2，颜色=主题色。
4. 12 条边上各一个能量脉冲光点 sprite 沿边奔跑，活跃度驱动速度与亮度。
接口：setActivity(0..1)（蓄力 0.3+p*0.7 / 主特效 1 / 重组衰减 / 待机 0.05+呼噜）
     triggerImpactBurst(count, strength)（随机墙面点生成错峰涟漪；
     主特效开始时按转场类型触发一波，相位 0.5 再补一小波）

═══════════════════════════════════════════
## 6. 九种转场（数字键 1-9 强制指定；随机选择且不与上次重复）
═══════════════════════════════════════════
公共时间轴：蓄力 0.3s（旧形态保持+高频振动渐强+容器边框脉冲）→ 主特效
（时长见各条）→ 弹性重组 0.5s。重组统一：per-particle 延迟 = seed*0.22，
easeOutBack(c=1.70158) 拉向新目标，落定时"抖毛"= curl(newTarget*2.2) *
sin(localT*31+seed*97) * 包络 * 0.42。颜色/主题色全程随 colorMix 渐变。
主特效阶段全程开启 4.3 碰撞（restitution 0.74）+ 4.4 环流。

1. 星云旋涡 0.85s：cheap 解析 curl 场（两组相位错开的 -cos 向量场叠加）×
   (7+p*5) + 水平切向涡旋力 + 径向收束，冲击波 bell(p,0.2,0.1)*27 外推撞壁。
2. 烟花 0.95s：先聚核（-position*46×衰减），bell(p,0.31,0.095)*(46+seed*20)
   按 9 个簇方向（黄金角分布）炸开，重力下坠+curl 余烬，撞壁四溅。
3. 黑洞 0.85s：吞噬段径向吸入(13/(r+0.12))+切向吸积盘+潮汐拉伸（x,z 压 y 拉）；
   position pass 里"过奇点即重生"一次性锁存（用 position.w 符号做锁），
   重生为半径 0.06+seed*0.16 的小云团（铁律 7），沿新目标方向反向喷涌。
4. 流沙 1.0s：按旧目标高度错峰释放，重力下落+微 curl，虚拟地板堆积
   （低于地板强力弹回+水平扩散），新猫按新目标高度自下而上"长"出。
5. 龙卷风 0.8s：绕竖轴螺旋（切向 8+p*22）+径向收束（1.2→12 渐紧）+
   上下抖动，bell(p,0.72,0.09)*30 骤停甩出撞壁，然后重组。
6. 流萤群 1.0s：12 个虚拟领航点（不同频率 sin/cos 轨道），每粒子跟随
   seed 对应的领航点+curl 漫游+呼吸明暗（targetEnergy 随 sin 闪烁），
   末段 25 倍力召回。O(N)，禁止邻居查找。
7. 故障闪现 0.65s：空间按 0.25 大小体素分块，帧率量化(18Hz)随机瞬移错位
   （position 域直接写，velocity 域只做阻尼），1/3 粒子按 RGB 三通道分离
   偏移+染色，配合后期色散脉冲，块状重排成新猫。
8. 海浪 1.05s：一堵波前墙从 x=-1.2H 推进到 +1.2H（相位 0.05→0.85）。
   设 d = position.x - waveFront：
   |d|<0.45 的粒子受"卷浪力" = 前推 4.0 + 抬升 bell(d,+0.12,0.2)*9 −
   下压 bell(d,-0.3,0.25)*6（水分子圆周轨迹感）+ curl×1.5 浪花乱流；
   波峰区（y>0.3 且 |d|<0.2）energy+=0.8 → 白色浪尖闪光；
   未过波粒子弱吸附旧猫(×6)，已过波吸附新猫(×smoothstep(0.6,1,p)*14)；
   波前每过 1/4 行程沿容器底边 triggerImpactBurst 一排涟漪。
9. 涟漪池 1.1s：
   p 0→0.45 旧猫熔化：目标=(x, -0.88H, z) 平面 + 同心波纹
   y += sin(r*18 - t*9)*0.03*exp(-r*1.5)，吸附力 12 → 猫化成一池发光水面；
   p 0.45→0.6 中心水柱：r<0.25 的粒子 bell(p,0.52,0.05)*40 向上喷射；
   p 0.6→1 新猫从水面自下而上生长：releaseAt = 0.6+(newTarget 归一高度)*0.25，
   到点的粒子强吸附新目标；6% 的粒子额外挂重力 -3 做"滴水"拖尾。
   全程护罩底面连发小涟漪，法阵亮度拉高——像猫从魔法水镜里升起。

═══════════════════════════════════════════
## 7. 模型管线
═══════════════════════════════════════════
- 读 manifest.json 列表；GLTFLoader + 本地 DRACOLoader/KTX2Loader
- 多网格模型：按各网格世界空间表面积分配采样数，MeshSurfaceSampler 均匀
  表面采样到固定粒子数（=档位数）
- 颜色优先级：顶点色 → UV 采 baseColor 贴图（注意 sRGB→线性）→ 材质色
  → 主题色。主题色 = 对采样颜色做加权平均后提饱和提亮（HSL S×1.4 L→0.62），
  作为该猫的 uTheme 与容器/法阵/氛围色
- 归一化：包围盒居中，最大边 fit 到 1.62（容器半边 1.03）
- 确定性采样：同一模型 5 万档是 20 万档的稳定前缀子集
- manifest 为空 → 程序化占位猫（球头+锥耳+椭球身+圆柱尾），保证 demo 可跑
- 首只猫加载完立即可玩，其余模型 2 秒后后台预载

═══════════════════════════════════════════
## 8. 手部交互（MediaPipe HandLandmarker，双手，VIDEO 模式）
═══════════════════════════════════════════
推理：Web Worker + GPU delegate（官方推荐，detectForVideo 会阻塞主线程），
约 26Hz；必须保留主线程降级实现（同接口），Worker 初始化失败自动切换。
镜像：原始帧送推理；屏幕坐标 x=1-x 翻转一次；handedness 交换一次；
内部只用"物理左右手"。
特征：手指伸直度 = 弦长/路径长 → smoothstep(0.56,0.94)；
openScore = (拇指×0.5 + 四指之和)/4.5；pinchDistance = 拇指-食指尖距/手掌尺寸
（手掌尺寸=(掌宽+掌高)/2）。
阈值（滞回）：open 进 0.72 出 0.56；fist 进 0.28 出 0.44；pinch 进 0.27 出 0.4。
确认帧：open 4 / fist 4 / pinch 3 / 双拳坍缩 2 / 切换张开 2。
冷却：切换 450ms，坍缩 1200ms。跟踪丢失 3 帧重置该手（滞回状态保留短暂宽限）。
One-Euro 滤波只用于连续量（掌心/朝向/张开度/捏点）；离散手势用未滤波值。
手势语义：
- 左掌张开：召唤容器（sticky 常驻，见铁律 9）；掌心=容器位置；掌朝向=微旋转；
  spread 死区公式见铁律 10
- 右手握拳→张开：切换下一只猫。握拳持稳时 HUD 提示"⚡拳已蓄力·张开换猫"
- 右手捏合(受铁律 8 约束)上下拖动：指数缩放容器 0.55~2.2
- 双拳（2 帧确认）：坍缩熄灭（粒子收成亮核后暗掉，容器淡出）；
  左掌完全松开再张开才能重新召唤
- 事件优先级：双拳 > 捏合 > 切换 > 左掌连续控制
- 转场进行中收到切换手势 → 排队一个，转场结束自动执行（禁止静默丢弃）

═══════════════════════════════════════════
## 9. 鼠标/触摸降级（摄像头拒绝时自动进入，功能完整）
═══════════════════════════════════════════
移动=容器跟随（微倾斜视差）；点击/轻触=换猫；滚轮=缩放；Shift+滚轮=散开度；
C=坍缩（再动一下复活）；H=隐藏 HUD；1-9=强制指定下一次转场。
HUD（左上小字）：FPS / 输入模式 / 当前手势 / 猫名 / 转场名 / GPU 格式。

═══════════════════════════════════════════
## 10. 性能
═══════════════════════════════════════════
桌面默认 20 万 60fps；DPR 上限 2；后期链单独的内部分辨率系数
（桌面 1.15 / 移动 0.85），帧预算超标时先降后期分辨率、不动粒子数；
用户手选的档位永不静默降级；切档 = 整套重建 GPGPU（采样序列复用）。

═══════════════════════════════════════════
## 11. 验收清单（逐条自测，每条附证据/截图）
═══════════════════════════════════════════
□ 纯黑色皮毛的猫清晰可见（呈主题色深发光体），纯白模型不烧成死白
□ 强制 9 种转场逐一截图：任何时刻粒子都在容器内（铁律 5）
□ 转场爆发时护罩被撞处出现扩散涟漪、六边形网格亮起
□ 法阵双环反向旋转，转场时明显加速增亮
□ 待机画面锐利无拖影（Afterimage 已禁用），转场有光轨
□ Shift+滚轮把散开度拉满：粒子云均匀散在容器内不穿墙（铁律 12 截图）
□ 握拳 10 帧不产生任何 pinch 事件（单元测试：紧拳=拇指尖贴食指尖的
  真实拳形 landmarks）
□ 容器召唤后把手放下：容器原地常驻不消失
□ 转场中连续触发切换：结束后自动补一次（排队），不丢
□ 镜像 handedness 单测：屏幕左侧的"物理右手"不触发召唤
□ 60fps@20万（桌面实测数值写进交付说明，不虚构）
□ 断网/屏蔽外域后完整可用（wasm/task/decoder/模型全本地）
□ 移动端视口自动 5 万档，触摸可完整操作
□ npm run 类型检查 + 单元测试 + 生产构建全部通过
□ 拒绝摄像头权限时 3 秒内进入鼠标模式且无报错死页

先输出实现计划（含文件清单），然后按阶段 A→E 实现。
每完成一个阶段，展示该阶段验收结果再继续。
